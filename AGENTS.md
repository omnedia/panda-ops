# Agent Instructions

This repository uses `.github/copilot-instructions.md` as the root AI instruction loader.

Agents must start there, then resolve the required `.github/ai/*` and `.github/guidelines/*` files before making code
changes.
